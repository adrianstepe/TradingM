{
  "name": "phantom-trading-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Telegram bot for multi-wallet Solana buy/sell control via Jupiter",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js"
  },
  "comment_overrides": "jayson (via @solana/web3.js) pins uuid 8.x, which trips GHSA-w5hq-g745-h8pq. The flaw is in uuid v3/v5/v6 when passed a buffer; jayson only calls v4(), so it is unreachable here. Pinned anyway to keep `npm audit` clean — remove once web3.js ships a newer jayson.",
  "overrides": {
    "uuid": "^11.1.1"
  },
  "dependencies": {
    "@solana/web3.js": "^1.95.3",
    "bs58": "^6.0.0",
    "dotenv": "^16.4.5",
    "telegraf": "^4.16.3"
  }
}
